{
  "name": "solar-fund",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build --network sepolia",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ solar-fund",
    "create-local": "graph create --node http://localhost:8020/ solar-fund",
    "remove-local": "graph remove --node http://localhost:8020/ solar-fund",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 solar-fund",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.51.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
